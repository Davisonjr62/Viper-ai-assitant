import{useEffect,useRef}from'react';import{useViperState}from'../../hooks/useViperState';import{STATE_CONFIG,type ViperSettings}from'../../core/types';import{viperEventBus}from'../../core/ViperEventBus';import{viperBackend}from'../../backend/ViperBackend';import{useAudioLevel}from'../../hooks/useAudioLevel';import'./ViperOrb.css';export function ViperOrb({settings}:{settings:ViperSettings}){const state=useViperState(),cfg=STATE_CONFIG[state],ref=useRef<HTMLDivElement>(null),mic=useAudioLevel('mic',state==='listening'),spk=useAudioLevel('speaker',state==='speaking'),level=state==='listening'?mic:spk;useEffect(()=>{const el=ref.current;if(!el)return;const down=(e:PointerEvent)=>{el.dataset.dragging='1';el.setPointerCapture(e.pointerId);};const move=(e:PointerEvent)=>{if(el.dataset.dragging==='1'){el.style.left=`${Math.max(0,e.clientX-160)}px`;el.style.top=`${Math.max(0,e.clientY-160)}px`}};const up=()=>{el.dataset.dragging='0'};el.addEventListener('pointerdown',down);el.addEventListener('pointermove',move);el.addEventListener('pointerup',up);return()=>{el.removeEventListener('pointerdown',down);el.removeEventListener('pointermove',move);el.removeEventListener('pointerup',up)}},[]);const test=()=>{viperEventBus.emit('COMMAND_RECEIVED');setTimeout(()=>viperEventBus.emit('FAST_COMMAND_STARTED'),150);setTimeout(()=>viperEventBus.emit('SPEECH_STARTED'),700);setTimeout(()=>viperEventBus.emit('SPEECH_COMPLETED'),1800)};return <div ref={ref} className={`viper-orb ${state}`} style={{'--size':`${settings.orbSize}px`,'--level':level,'--glow':cfg.glowIntensity*settings.glowIntensity} as React.CSSProperties} onDoubleClick={test}><div className="rings"><i/><i/><i/></div><div className="particles">{Array.from({length:16},(_,i)=><b key={i} style={{'--i':i} as React.CSSProperties}/>)}</div><div className="core"><span>⌄</span></div>{settings.showStatusText&&cfg.statusLabel&&<small>{cfg.statusLabel}</small>}</div>}
