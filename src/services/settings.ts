import{DEFAULT_SETTINGS,type ViperSettings}from'../core/types';declare global{interface Window{viperNative?:{getSettings:()=>Promise<ViperSettings>;setSetting:(k:string,v:unknown)=>Promise<boolean>;}}}export const settingsService={async load(){if(window.viperNative)return {...DEFAULT_SETTINGS,...await window.viperNative.getSettings()};try{return{...DEFAULT_SETTINGS,...JSON.parse(localStorage.getItem('viper-settings')||'{}')}}catch{return DEFAULT_SETTINGS}},onChanged(_cb:(p:Partial<ViperSettings>)=>void){return()=>{}}};
